const languages = ['html', 'css', 'javascript', 'react', 'node', 'postgres'];
console.log(languages);

var firstElement = languages[0];
var elementOne = 'the first element of the array is ';
console.log(elementOne + firstElement);

var thirdElement = languages[2];
var elementThird = 'the 3rd element of the array is ';
console.log(elementThird + thirdElement);

var length = languages.length;
var lengthSentence = 'The length of the array is ';
console.log(lengthSentence + length);

var lastElement = languages[languages.length - 1];
var lastElementSentence = 'The last element of the array is ';
console.log(lastElementSentence + lastElement);
